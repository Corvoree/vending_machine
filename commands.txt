git remote add origin
git push -u origin --all
(проект с гитхаба локально как Дмитрий показал на вебинаре через идею)

git branch
(ветка уже была в репозитории с заданием)

git status
посмотреть состояния файлов

git add
отслеживать файл или папку

git rm --cached
убрать файл из индексации

git merge
объеденить ветки

git checkout
переключение между ветками

git commit -m
создать коммит через и добавть описание к нему через ""

git log
посмотреть коммиты

ls -la
список файлов

cd
назад


